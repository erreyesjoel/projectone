/* este js es para el menu, para abrir y cerrarlo */
const toggleMenu = () => {
  const menu = document.getElementById("menuDropdown");
  menu.style.display = menu.style.display === "block" ? "none" : "block";
};
